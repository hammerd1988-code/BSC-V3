{
  "name": "casper-ssh-mobile",
  "version": "1.0.0",
  "main": "index.ts",
  "dependencies": {
    "@dylankenneally/react-native-ssh-sftp": "^1.9.0",
    "expo": "~57.0.9",
    "expo-status-bar": "~57.0.1",
    "react": "19.2.3",
    "react-native": "0.86.2"
  },
  "devDependencies": {
    "@types/react": "~19.2.2",
    "typescript": "~6.0.3"
  },
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "web": "expo start --web",
    "typecheck": "tsc --noEmit",
    "lint": "npm run typecheck",
    "build:apk": "expo prebuild --platform android && cd android && ./gradlew assembleRelease"
  },
  "private": true
}
