node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# Google service-account / OAuth credentials — never commit these
.google-credentials/
*.json.key
service-account*.json

# Capacitor iOS project — generated on demand on macOS (CocoaPods), not committed.
# The Android project IS committed (builds on any OS incl. Linux CI).
/ios/
