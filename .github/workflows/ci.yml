name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          # Keep the job token out of .git/config so dependency lifecycle
          # scripts can't read it.
          persist-credentials: false

      # A conflict "resolution" that commits the markers themselves should
      # never survive to the expensive steps — or to review.
      - name: Check for leftover merge conflict markers
        run: |
          if git grep -nE '^(<{7}|>{7})( |$)' -- .; then
            echo '::error::Unresolved merge conflict markers are committed to the tree'
            exit 1
          fi

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - run: npm ci --include=dev

      - name: Install Casper SSH mobile dependencies
        run: npm --prefix packages/casper-ssh-mobile ci --include=dev

      - name: Typecheck
        run: npm run lint

      - name: Typecheck Casper SSH mobile
        run: npm run lint:mobile

      - name: Test
        run: npm run test:run

      - name: Build
        run: npm run build

      # Typecheck, tests, and `vite build` never execute server.ts, so a
      # server that dies at import time passes all of them. Boot the real
      # entrypoint against stub credentials and require /api/health to answer.
      - name: Boot smoke test (server.ts serves /api/health)
        run: bash scripts/ci-smoke.sh
